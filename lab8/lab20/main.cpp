#include <iostream>
#include <string>
using namespace std;

int dlugosctekstu(string *tekst)
{
			    int liczba = 0;
			    for(unsigned int i = 0; i < tekst(i); i++) {
			        if (!isspace(in[i]))
			            ++liczba;
			    }
			  //  return count;
   // cout <<"Dlugosc bez spacji wynosi: " << dlugosctekstu(tekst)<< endl;
}
int main()
{
    string tekst ="Hello World";
    cout << tekst << endl;
    dlugosctekstu(string in);
    cout <<"Dlugosc bez spacji wynosi: " << liczba << endl;
    return 0;
}
